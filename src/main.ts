function multiplicar(n1: number, n2: number): number {
    return n1 *n2;
}

function saldar(nome: string): string {
    return `Olá ${nome}`
}

console.log(multiplicar(2, 3));
console.log(saldar('Anderson'));